name: Build APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew assembleDebug
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug.apk
        path: app/build/outputs/apk/debug/app-debug.apk
        
ہدایات
فائل اپ ڈیٹ کریں: .github/workflows/android.yml فائل میں اوپر دیے گئے کوڈ کو پیسٹ کریں۔
پش کریں:
git add .
git commit -m "Update workflow to v4"
git push https://AdnanHafiz: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@github.com/AdnanHafiz/IslamicDigitalWall.git main
                
